import { extendTheme } from '@chakra-ui/react'
const theme = extendTheme({
  colors: {
    midBlue: {
      100: '#1E1482',
      300: '#4664FA',
      200: '#323CBE',
    },
  },
})

export default theme
